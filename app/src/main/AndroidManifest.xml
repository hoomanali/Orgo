<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.orgo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/slug_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/slug_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".views.DiscussionViewActivity"></activity>
        <activity android:name=".views.OrganizationProfilePage" />
        <activity android:name=".views.FirebaseTestActivity" />
        <activity android:name=".views.ViewGroupActivity" />
        <activity android:name=".views.ViewUserGroups" />
        <activity android:name=".views.MainNavActivity" />
        <activity android:name=".views.RegisterPage" />
        <activity android:name=".views.LoginPage" />
        <activity android:name=".views.OrganizationPage" />
        <activity android:name=".views.GroupPage" />
        <activity android:name=".views.CreateGroupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>